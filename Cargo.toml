[package]
name = "sharp-cli"
version = "0.1.0"
edition = "2024"
description = "Generate optimized ClickHouse schemas for event analytics workloads"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
thiserror = "2"
